# Copy this file to .env.local and edit the value.
# Public site URL (no trailing slash): used for sitemap, canonical URLs and Open Graph.
# Local:      http://localhost:3000
# Production: https://your-domain.com  (set it in Vercel > Project > Settings > Environment Variables)
NEXT_PUBLIC_SITE_URL=http://localhost:3000